# COSMOS-C5-20260916 version 1

Frozen before C5 execution: 2026-09-16T03:32:32.108736+00:00

Question: for every physical row of the two pinned Cosmos3-DROID splits, does `index = dataset_from_index[episode_index] + frame_index` hold? This is a new row-wise alignment check C5. It does not rename or overwrite historical C1–C4; a historical code comment called a different summary comparison C5, which is not this experiment.

Inputs: `INPUTS.json` pins six complete episode-table objects by SHA-256 and revision. All 74 frame paths are derived from those episode tables, ordered split/chunk/file, and exactly three columns are freshly read using HTTP ranges: episode_index, index, frame_index. Expected complete population: 71,907 episodes and 22,412,712 frame rows. The old report supplies a coverage target, never C5 outcomes. Metadata can be recovered from public URLs; an explicitly provided cache is accepted only after full-file digest/size verification. Full frame Parquet objects are not downloaded or hashed. Record each fetched range's offset/length/SHA-256 plus a canonical little-endian int64 digest of the decoded columns in physical row order.

An input or execution error yields INCOMPLETE/EXECUTION_ERROR, never a scientific pass. Check unknown/duplicate/missing episode identities and declared row counts as coverage safeguards, report them separately from C5 violations. C5 violations are rows and affected episodes; log up to 10 examples per file. Full C5 PASS requires all expected files, episodes and rows, no integrity errors, and no violating rows. A nonzero C5 outcome is CHECK_FAILED (an executed scientific result), not runtime failure. Integer equality is exact, no tolerance. The check does not prove physical row ordering, timestamp/video/action correspondence or model harm.

Preserve a run directory with results, range receipts, input/code/protocol digests, dependency versions and stdout/stderr. Never overwrite an existing run. Budget: at most 1 GiB of frame range response bodies per invocation, measured at the HfFileSystem fetcher; metadata downloads, HTTP headers and path-resolution traffic are reported separately or excluded explicitly. No model training, video decoding or tensor columns.

Optional consumer replay preselection: independently of C5 outcomes, choose in each split the lowest episode_index whose three episode summary values (min/max/mean) disagree with episode_index, and the lowest agreeing episode. Freeze their IDs in a separate protocol before executing any consumer output. Compare original inputs against copies changing only stats/episode_index/{min,max,mean,q01,q10,q50,q90,q99} to the constant frame episode identity. Execute the exact pinned LeRobot update_meta_data function (not the full merge); fixed offsets and same frame identities in both conditions. This is a constructed intervention on actual records, not a ninth historical defect.
